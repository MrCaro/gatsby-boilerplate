import React from 'react'

const Header = () => {
    return (
        <nav className="bg-primary py-4">
            <span>logo</span>
            <span>search...</span>
        </nav>
    )
}

export default Header
